---
import HeaderLink from './HeaderLink.astro';
// Conditional Admin Link Component
// This component will show the Admin link only when:
// 1. User is authenticated via Netlify Identity
// 2. User has admin role/permissions
// 
// CURRENT STATUS: DISABLED (Phase 2 - Post Launch)
// To enable authenticated admin access:
// 1. Set ENABLE_AUTHENTICATED_ADMIN = true
// 2. Configure Netlify Identity with admin roles
// 3. Uncomment the conditional rendering below
// 4. Add authentication logic to check user roles

// TODO: Enable this after setting up Netlify Identity with admin roles
const ENABLE_AUTHENTICATED_ADMIN = false;

// TODO: Add authentication check logic here
// Example:
// const isAuthenticated = await checkNetlifyIdentity();
// const hasAdminRole = await checkUserRole('admin');
// const showAdminLink = ENABLE_AUTHENTICATED_ADMIN && isAuthenticated && hasAdminRole;

const showAdminLink = false; // Currently disabled for security
---

{showAdminLink && (
	<HeaderLink href="/admin" class="admin-link">Admin</HeaderLink>
)}

<style>
	/* Admin link specific styles */
	.admin-link {
		/* Add any special styling for admin link here */
		position: relative;
	}
	
	.admin-link::after {
		content: '🔒';
		position: absolute;
		top: -2px;
		right: -2px;
		font-size: 0.6rem;
		opacity: 0.7;
	}
</style> 