---
// Simple sticky test page to isolate the issue
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sticky Test</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      background: #1a1a1b;
      color: white;
      font-family: Arial, sans-serif;
      /* CRITICAL: No overflow restrictions */
      overflow: visible;
    }
    
    .test-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
      display: flex;
      gap: 30px;
    }
    
    .main-content {
      flex: 1;
      background: #2a2a2b;
      padding: 20px;
      border-radius: 8px;
    }
    
    .sidebar {
      width: 300px;
      background: #3a3a3b;
      padding: 20px;
      border-radius: 8px;
      /* BASIC STICKY POSITIONING */
      position: sticky;
      top: 20px;
      height: fit-content;
    }
    
    .content-block {
      background: #4a4a4b;
      margin: 20px 0;
      padding: 40px;
      border-radius: 8px;
    }
    
    .red-test {
      position: sticky;
      top: 10px;
      background: red;
      color: white;
      padding: 15px;
      margin: 20px 0;
      text-align: center;
      font-weight: bold;
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <h1 style="text-align: center; padding: 20px; background: #0066ff;">Sticky Position Test</h1>
  
  <div class="red-test">
    🔴 RED STICKY TEST - Should stick at top: 10px
  </div>
  
  <div class="test-container">
    <div class="main-content">
      <h2>Main Content Area</h2>
      
      <div class="content-block">
        <h3>Content Block 1</h3>
        <p>This is a test content block. Scroll down to see if the sidebar sticks.</p>
      </div>
      
      <div class="content-block">
        <h3>Content Block 2</h3>
        <p>More content to create scrollable area. The sidebar should stay visible as you scroll.</p>
      </div>
      
      <div class="content-block">
        <h3>Content Block 3</h3>
        <p>Even more content. If sticky is working, the sidebar should follow you.</p>
      </div>
      
      <div class="content-block">
        <h3>Content Block 4</h3>
        <p>Keep scrolling to test the sticky behavior.</p>
      </div>
      
      <div class="content-block">
        <h3>Content Block 5</h3>
        <p>Final content block. The sidebar should still be visible.</p>
      </div>
      
      <div style="height: 150vh; background: linear-gradient(45deg, #333, #666); display: flex; align-items: center; justify-content: center; border-radius: 8px;">
        <h2>Tall Section - Scroll Test Area</h2>
      </div>
    </div>
    
    <div class="sidebar">
      <h3>🟢 SIDEBAR TEST</h3>
      <p>This sidebar should stick to the top as you scroll.</p>
      <div style="background: #00ff00; color: black; padding: 10px; margin: 10px 0; border-radius: 4px;">
        <strong>Sticky Test</strong><br>
        If this stays visible while scrolling, sticky is working!
      </div>
      <p>Sidebar content continues here...</p>
      <div style="height: 200px; background: #666; margin: 20px 0; display: flex; align-items: center; justify-content: center; border-radius: 4px;">
        More sidebar content
      </div>
    </div>
  </div>

  <script>
    // Test sticky behavior
    document.addEventListener('DOMContentLoaded', function() {
      const sidebar = document.querySelector('.sidebar');
      const redTest = document.querySelector('.red-test');
      
      function logStickyStatus() {
        const sidebarComputed = window.getComputedStyle(sidebar);
        const redTestComputed = window.getComputedStyle(redTest);
        
        console.log('Sidebar sticky status:', {
          position: sidebarComputed.position,
          top: sidebarComputed.top,
          zIndex: sidebarComputed.zIndex
        });
        
        console.log('Red test sticky status:', {
          position: redTestComputed.position,
          top: redTestComputed.top,
          zIndex: redTestComputed.zIndex
        });
        
        // Check for sticky blockers in parent elements
        let parent = sidebar.parentElement;
        let level = 1;
        while (parent && level <= 3) {
          const parentComputed = window.getComputedStyle(parent);
          console.log(`Parent ${level} (${parent.tagName}):`, {
            overflow: parentComputed.overflow,
            transform: parentComputed.transform,
            contain: parentComputed.contain,
            filter: parentComputed.filter
          });
          parent = parent.parentElement;
          level++;
        }
      }
      
      // Log after page load
      setTimeout(logStickyStatus, 500);
      
      // Test on scroll
      window.addEventListener('scroll', function() {
        const scrollY = window.scrollY;
        document.title = `Scroll: ${scrollY}px - Sticky Test`;
      });
    });
  </script>
</body>
</html>
